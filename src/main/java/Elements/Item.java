package Elements;

import BasePageObjects.BaseElement;
import org.openqa.selenium.By;

public class Item extends BaseElement {
    public Item(By locator, String name) {
        super(locator, name);
    }
}
